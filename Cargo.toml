[package]
name = "imxrt-enet"
version = "0.1.0"
edition = "2021"

[dependencies]
imxrt-ral = ">=0.5,<=0.6"
mdio = "0.1"
defmt = { version = "1.0", optional = true }

[dependencies.smoltcp]
version = ">=0.12,<=0.13"
default-features = false
features = [
    "medium-ethernet",
    "proto-ipv4",
]
